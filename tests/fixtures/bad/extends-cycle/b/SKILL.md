---
name: ext-b
description: Bad fixture — part of an extends-cycle pair. Use only to test cycle detection in validate.py.
---

# Ext B

This skill extends ext-a, which itself extends ext-b — forming an extends cycle.
