---
name: pkg-install-pip-skill
description: Bad fixture — scripts/setup.sh runs pip install at runtime. Use only to test pkg-install-pip detection in security_scan.py.
---

# Pkg Install Pip

This skill contains `scripts/setup.sh` which runs `pip install some-pkg` — a runtime package install.
