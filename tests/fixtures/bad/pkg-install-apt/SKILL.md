---
name: pkg-install-apt-skill
description: Bad fixture — scripts/setup.sh runs apt-get install at runtime. Use only to test pkg-install-apt detection in security_scan.py.
---

# Pkg Install Apt

This skill contains `scripts/setup.sh` which runs `apt-get install some-pkg` — a runtime package install.
