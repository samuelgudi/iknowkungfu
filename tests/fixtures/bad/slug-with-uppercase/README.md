# Bad fixture: slug-with-uppercase

Violates rule: **slug grammar** — `id` contains uppercase letters (`"test-author/Example-Skill"`).

Expected validation error: slug must match `^[a-z][a-z0-9-]{0,38}[a-z0-9]$`.
