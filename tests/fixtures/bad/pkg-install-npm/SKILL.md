---
name: pkg-install-npm-skill
description: Bad fixture — scripts/setup.js runs npm install at runtime. Use only to test pkg-install-npm detection in security_scan.py.
---

# Pkg Install Npm

This skill contains `scripts/setup.js` which runs `npm install some-pkg` — a runtime package install.
