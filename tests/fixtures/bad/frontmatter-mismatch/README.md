# Bad fixture: frontmatter-mismatch

Violates rule: **frontmatter mismatch** — SKILL.md frontmatter `name: other-name` does not match the slug in `meta.json` `id: "test-author/example"`.

Expected validation error: SKILL.md `name` must match the slug portion of `meta.json` `id`.
