---
name: nested-git-skill
description: Bad fixture — contains a .git directory at the skill root. Use only to test nested-git detection in validate.py.
---

# Nested Git

This skill directory contains a `.git/HEAD` file directly inside the skill root.
