{
  "yanks": [
    {
      "id": "samuelgudi/semver-bump-decider",
      "version": "0.1.0",
      "reason": "v0.1.0 content-hash drift: the tree pinned by versions[0.1.0].sha predates the author.name normalization (`samuelgudi` -> `Samuel Gudi`) that landed during the iknowkungfu v0.1.2 pre-publication audit, so install_skill's git-archive of that SHA hashes to a value that no longer matches the regenerated source.content_hash. Use 0.1.1 — same content, hash-consistent."
    }
  ]
}
